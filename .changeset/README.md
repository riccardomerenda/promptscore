This folder stores Changesets release metadata.

- Add a changeset with `npm run changeset` for user-visible changes.
- Release automation versions packages from these files, updates changelogs, and then removes them.
